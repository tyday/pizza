let thisvariable=0;

function selectMenuItem(li, itemName, itemDescription,itemToppings){
    console.log(li, itemName, itemDescription, itemToppings);
    // overlay = document.createElement('div');
    // card = document.createElement('div')
    // overlay.classList.add('overlay')
    // document.body.appendChild(overlay)
    // card_form = document.createElement

}